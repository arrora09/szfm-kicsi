import { useState } from "react";
import "../index.css";

export const NotesMain = () => {
  const [notes, setNotes] = useState([]);
  return <div className={"w-screen h-screen bg-black"}>oi</div>;
};
